"use strict";

var text = "Hola";
var textUppercase = text.toUpperCase(); // aquí ponemos paréntesis al final del método

var texto = "Holas";
var textLength = text.length; // aquí no usamos paréntesis porque esto es una propiedad, no un método.
//   metodos vs propiedades

/* endswitch
indexOf primer indice que encuentra
lastIndesxOf ultimo indice que encuentra
Slice para encontre un indice entre 2 posiciones
replace remplaza el primer indice por el que le indiquemos "a" , "b"
includes para preguntar si el texto incluye algun valor que le preguntamos */

console.log(text.includes("a"));