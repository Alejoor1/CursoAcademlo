const age = 30;
const b = age > 18;
// > mayor que
// < menor que    
// === igual que
// !== distinto de
// >= mayor o igual
// <= menor o igual

console.log(age >= 18);
console.log(typeof b);
